apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blur-detection.fullname" . }}-test-connection"
  labels:
    {{- include "blur-detection.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blur-detection.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
